/**
 * v3.5: TemplateKey replaced by CanvasDefinition (custom templates persisted in DB).
 * This file is kept as a thin shim to avoid breaking any residual import.
 * @deprecated Import from @/types/template instead.
 */
export type TemplateKey = never;
