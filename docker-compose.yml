services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: gs1udi
      POSTGRES_USER: gs1user
      POSTGRES_PASSWORD: gs1pass
    ports:
      - "5432:5432"
    volumes:
      - postgres_dev_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U gs1user -d gs1udi"]
      interval: 5s
      timeout: 5s
      retries: 10

  backend:
    image: python:3.12-slim
    working_dir: /app/backend
    volumes:
      - ./:/app
      - /app/backend/.venv
    environment:
      DATABASE_URL: postgresql+asyncpg://gs1user:gs1pass@postgres:5432/gs1udi
      CORS_ORIGINS: http://localhost:3000,http://127.0.0.1:3000
      REDIS_URL: redis://redis:6379
      ENABLE_AUTH: ${ENABLE_AUTH:-true}
      JWT_SECRET: ${JWT_SECRET:-dev-secret-change-in-production}
      RESEND_API_KEY: ${RESEND_API_KEY:-}
      RESEND_FROM_EMAIL: ${RESEND_FROM_EMAIL:-onboarding@resend.dev}
    command:
      - bash
      - -lc
      - |
        set -e
        pip install uv -q
        uv sync
        uv run alembic upgrade head
        uv run uvicorn main:app --host 0.0.0.0 --port 8000 --reload
    ports:
      - "8000:8000"
    healthcheck:
      test: ["CMD", "python3", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/api/v1/health')"]
      interval: 10s
      timeout: 5s
      retries: 15
      start_period: 60s
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_started

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5

  frontend:
    image: node:22-alpine
    working_dir: /app/frontend
    volumes:
      - ./:/app
      - /app/frontend/node_modules
    environment:
      CI: "true"
      BACKEND_URL: http://backend:8000
      NEXT_PUBLIC_ENABLE_AUTH: ${ENABLE_AUTH:-true}
    command: ["sh", "-lc", "corepack enable && pnpm install && pnpm dev --hostname 0.0.0.0 --port 3000"]
    ports:
      - "3000:3000"
    depends_on:
      backend:
        condition: service_healthy

volumes:
  postgres_dev_data:
