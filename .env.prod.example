# 前端/后端对外暴露端口
FRONTEND_PORT=3001
BACKEND_PORT=18000

# 后端 worker 数（建议 = CPU核数 × 2 + 1）
WORKERS=4

# CORS：允许前端访问后端，填写服务器 IP 或域名
CORS_ORIGINS=http://YOUR_SERVER_IP:3001,https://YOUR_DOMAIN

# PostgreSQL（建议修改密码）
POSTGRES_DB=gs1udi
POSTGRES_USER=gs1user
POSTGRES_PASSWORD=change_me_in_production
